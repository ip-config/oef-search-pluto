syntax = "proto3";

// One day this will support paging, size indicators and "more" flags.

message DapResult {
  message OEFCore {
    string address = 1;
    double score = 2;
  }
  repeated OEFCore oef = 1;
}
